version: '3'

services:
  postgres-ecommerce:
    image: postgres:15.3
    restart: always
    ports:
      - 5437:5432
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    volumes:
      - ./postgres:/var/lib/postgresql/data
